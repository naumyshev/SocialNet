import React from 'react';
import logo from "../pic/logo512.png";

export const Header = () => {
    return (
        <header className='header'>
            <img src={logo} alt="logo"/>
        </header>
    );
};

